const letter = ["a", "b", "c"];
const number = [1, 2, 3];


const combine = letter.concat(number); //! this method is used to combine two or more arrays and return a new array
console.log(combine);
console.log(letter); // ! this method does not change the original array
console.log(number); // ! this method does not change the original array
